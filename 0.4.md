TESTING DIAGRAM
```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_notes
    Note right of server: the user input in the paylod
    activate server
    server-->>browser: HTML code (redirect) status code :300
    deactivate server

```
